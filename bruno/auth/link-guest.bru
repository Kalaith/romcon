meta {
  name: link-guest
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/link-guest
  body: json
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "guest_user_id": "{{guest_user_id}}"
  }
}
